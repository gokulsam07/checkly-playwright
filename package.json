{
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/node": "^25.3.2",
    "dotenv": "^17.3.1"
  },
  "scripts": {
    "ui": "playwright test --config=playwright-config.ts --project=chrome",
    "api": "playwright test --config=playwright-config.ts --project=api"
  }
}
